{
  "name": "roadtrip",
  "description": "Client-side routing library. It's about the journey, not just the destination",
  "author": "Rich Harris",
  "version": "0.1.6",
  "license": "MIT",
  "main": "dist/roadtrip.js",
  "jsnext:main": "src/roadtrip.js",
  "devDependencies": {
    "gobble": "^0.7.9",
    "gobble-babel": "^4.0.1",
    "gobble-cli": "^0.3.5",
    "gobble-esperanto-bundle": "^0.1.7",
    "gobble-sorcery": "^0.2.5",
    "jsdom": "^4.0.4",
    "mocha": "^2.2.1",
    "simulant": "^0.1.3"
  },
  "scripts": {
    "test": "mocha",
    "build": "set -e; npm test; mkdir -p dist; rm -rf dist/*; cp .tmp/roadtrip.* dist/; rm -rf .tmp",
    "prepublish": "npm run build"
  }
}
